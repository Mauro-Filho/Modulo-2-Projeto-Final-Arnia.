import styled from 'styled-components'
import { colorTheme } from '../../theme'

export const Wrapper = styled.div`
  background-color: ${colorTheme.backgroundLoggedPages};
  width: 100%;
  right: 0px;
  min-height: 100vh;
`
